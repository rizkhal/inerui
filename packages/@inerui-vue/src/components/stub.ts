import { defineComponent, h } from "vue";

export const Icon = defineComponent({
    setup() {
        return () =>
            h("span", { class: "" }, [
                //
            ]);
    },
});
